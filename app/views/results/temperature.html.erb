<div class="row" id="data_graph">
	<div class="col-md-6 col-md-offset-3">
		<%= image_tag("/assets/#{@user.id}_temperatures.png") %>
	</div>
</div>


<div class="row" id="data_table">
	<div class="col-md-6 col-md-offset-3">
		<h2> Recorded values: </h2>
		<% if @temps.count > 0 %>
		  <table class="table table-hover">
		    <thead>
		      <tr>
		        <th>Temp (ºF)</th>
		      </tr>
		    </thead>
		    <tbody>
		    <% @temps.each do |temp| %>
		      <tr>
		        <td><%= temp %></td>
		      </tr>
		    <% end %>
		    </tbody>
		  </table>
		<% end %>
	</div>
</div>
		